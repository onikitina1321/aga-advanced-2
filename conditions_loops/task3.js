// #3.1
const number = 5

// #3.2

for (let i = 0; i < 11; i++) { console.log(number + ' ' + `* ${i}` + ' ' + '=', `${number * i}`) }

// #3.3
let index = 10

while (index >= 0) {
  console.log(number + ' ' + `* ${index}` + ' ' + '=', `${number * index}`)
  index--
}
